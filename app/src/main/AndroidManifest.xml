<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.startsmake.llrisetabbardemo">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".global.App"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/icon_dimen"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- activity有EditText时禁止自动弹出软键盘 -->
        <!-- 声明Activity时加上：android:windowSoftInputMode="stateHidden" -->
        <!-- 解决软键盘将布局顶起方法：android:windowSoftInputMode="adjustPan|stateHidden" -->
        <activity
            android:name=".activity.MainActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.HomeQRCodeActivity" />
        <activity android:name=".activity.HomeMsgActivity" />
        <activity android:name=".activity.SortActivity" />
        <activity android:name=".activity.MineDrawerActivity" />
        <activity android:name=".publish.PublicGoodsActivity" />
        <activity
            android:name=".publish.PublicActivityActivity"
            android:windowSoftInputMode="adjustPan|stateHidden" />
    </application>

</manifest>